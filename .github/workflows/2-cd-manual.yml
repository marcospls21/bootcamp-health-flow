# ... (após o Passo 1 de Apply Infra Core)

      - name: 🛰️ Update Kubeconfig
        if: ${{ inputs.action == 'apply' }}
        run: |
          aws eks update-kubeconfig --region us-east-1 --name ${{ secrets.EKS_CLUSTER_NAME }} 
          # Substitua o nome acima pelo nome real do seu cluster se for fixo

      # PASSO 2: Agora o Terraform terá o contexto correto para o kubectl
      - name: 2️⃣ Terraform Apply - Full Stack (Apps & Monitoring)
        if: ${{ inputs.action == 'apply' }}
        run: |
          echo "🚀 Instalando Aplicações e Monitoramento..."
          terraform apply -auto-approve
        working-directory: ./terraform